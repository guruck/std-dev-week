spring:
  datasource:
    url: jdbc:h2:mem:sdw2024 # ;TRACE_LEVEL_FILE=4
    driverClassName: org.h2.Driver
    username: sdw2024
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create-drop # validate / update / create / create-drop
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
